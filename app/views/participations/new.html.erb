<%= tag.h1 t(".heading"), class: "mb-4 font-bold" %>
<% if Participant.max_reached? %>
  <%= render "participants/max_reached" %>
<% else %>
  <%= render "form", participant: @participant %>
<% end %>
