<div class="flex flex-col items-center gap-2">
  <%=
    link_to t("register"),
            :register,
            class: "inline-block cursor-pointer rounded-lg bg-teal-300 px-4 py-2 text-3xl font-bold hover:bg-teal-400"
  %>
  <div>
    <div><%= t(:spots_available, count: Participant::MAX_PARTICIPANTS) %></div>
    <div><%= t(:spots_left, count: Participant.spots_left) %></div>
  </div>
</div>
