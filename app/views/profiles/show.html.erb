<div class="flex flex-col gap-16">
  <% if @next_tournament.present? %>
    <% if @next_participation.present? %>
      <%= turbo_frame_tag @next_participation, src: @next_participation, target: "_top" %>
    <% else %>
      <div class="flex justify-center text-center">
        <%= render "shared/register_button", tournament: Tournament.next %>
      </div>
    <% end %>
  <% end %>

  <div><%= turbo_frame_tag :account, src: :account, target: "_top" %></div>
</div>
