<%= render "tournaments/heading", tournament: @game.tournament, sub_heading: t(".enter_results") %>

<div class="flex flex-col gap-8">
  <%= render "intro", game: @game %>
  <%= render "form", tally: @tally, game: @game %>
</div>
